/**
 * Preview playback panel — renders the current timeline output. Stub for now.
 */
export function PreviewPanel() {
  return (
    <section className="flex h-full items-center justify-center bg-black/90 text-gray-400">
      <span className="text-sm">Preview (stub)</span>
    </section>
  );
}
